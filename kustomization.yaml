apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: task-scheduler
resources:
  - k8s/base
configMapGenerator:
  - name: init-sql-file
    files:
      - init.sql
generatorOptions:
  disableNameSuffixHash: true
